apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    eventlistener: collector-addon-listener-interceptor
  name: collector-addon-listener-el
spec:
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-scollector-addon-listener-interceptor
