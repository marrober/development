kind: Service
apiVersion: v1
metadata:
  name: two
  namespace: right
  labels:
    app: two
    app.kubernetes.io/component: two
    app.kubernetes.io/instance: two
spec:
  ipFamilies:
    - IPv4
  ports:
    - name: 8080-tcp
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    deployment: two
