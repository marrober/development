apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    eventlistener: network-event-interceptor-listener-interceptor
  name: network-event-interceptor-listener-el
spec:
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-network-event-interceptor-listener-interceptor
