apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerTemplate
metadata:
  name: network-event-interceptor-template
spec:
  params:
  - name: gitrepository.url
  - name: gitrevision
  - name: branch
  resourcetemplates:
  - apiVersion: tekton.dev/v1beta1
    kind: PipelineRun
    metadata:
      generateName: network-event-interceptor-
    spec:
      serviceAccountName: pipeline
      pipelineRef:
        name: network-event-interceptor
      params:
        - name: GIT_REPO
          value: $(tt.params.gitrepository.url)
        - name: GIT_REVISION
          value: $(tt.params.gitrevision)
        - name: GIT_REPO_NAME
          value: development
        - name: app-dir
          value: development/openshift/network-event-interceptor/application/app
        - name: dockerfile
          value: Dockerfile
        - name: image-short-name
          value: network-event-interceptor
        - name: imagestream-url
          value: default-route-openshift-image-registry.apps.ocp4.mr-openshift.co.uk/$(context.taskRun.namespace)/network-event-interceptor
          ## Update Kustomize file for the deployment of the application to development      
        - name: repo_type
          value: github.com
        - name: cd-git-url
          value: marrober/development
        - name: cd-git-repo-name
          value: development
        - name: cd-git-repo-path-to-content-development
          value: openshift/network-event-interceptor/application/deploy
        - name: cd-git-commit-comment-dev
          value: "Update from ci process - dev"
        - name: cd-git-user-name
          value: "Mark Roberts"
        - name: cd-git-user-email
          value: "mroberts@redhat.com"
        - name: quay-instance-url
          value: "quay.io"
        - name: quay-account
          value: "marrober"
        - name: quay-repository
          value: "network-event-interceptor-collector" 



      workspaces:
      - name: files
        volumeClaimTemplate:
          spec:
            accessModes:
            - ReadWriteOnce
            resources:
              requests:
                storage: 20Gi
      - name: resources
        volumeClaimTemplate:
          spec:
            accessModes:
            - ReadWriteOnce
            resources:
              requests:
                storage: 5Gi
